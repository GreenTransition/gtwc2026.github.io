      <table class="table table-striped">
        <tbody>
          {% for date in site.data.dates %}     
            <tr>
              <td style="text-align: left">{{ date.name }}: </td><td style="text-align: left">{{ date.date }}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>      
 